services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - AI_PROVIDER=mock
    volumes:
      - ./sample-documents:/app/sample-documents
      - ./data:/app/data

