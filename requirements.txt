# --- Core Azure AI Foundry / agent ---
# Pinned to the validated GA line. The 2.x/beta rewrite changes the agents
# surface (e.g. removes client.agents.threads), which breaks src/agent/agent.py.
azure-ai-projects==1.0.0
azure-ai-agents==1.1.0
azure-identity>=1.17.0

# --- Knowledge / retrieval ---
# Indexed SharePoint knowledge source uses the 2026-05-01-preview Search REST
# API via `requests` (no extra dep). For the typed SDK path instead, install the
# preview: pip install --pre azure-search-documents
azure-search-documents>=11.6.0b4
azure-storage-blob>=12.20.0
azure-keyvault-secrets>=4.8.0

# --- Multimodal ingestion ---
azure-ai-documentintelligence>=1.0.0b4
# Speech transcription via Azure AI Speech batch REST (uses 'requests'); SDK optional:
# azure-cognitiveservices-speech>=1.40.0

# --- Responsible AI / evaluation ---
azure-ai-contentsafety>=1.0.0
azure-ai-evaluation>=1.0.0

# --- API / frontend serving ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Utilities ---
requests>=2.32.0
python-dotenv>=1.0.1
tenacity>=8.4.0

# --- Dev / test ---
pytest>=8.2.0
